[project]
name = "ai-resume-agent"
version = "1.0.0"
description = "End-to-End Job Application Agent with format-preserving resume tailoring"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
